# Self-Supervised-Image-Representation-Learning-using-Masked-Autoencoders-MAE-
I recently completed a project where I implemented a Masked Autoencoder (MAE) from scratch using PyTorch to learn visual representations without labeled data. Instead of traditional supervised learning, this model learns by reconstructing images after masking a large portion of them.
